2018.5.20
1、SPI1->STAT 中的SSD和SSA写1清不了零；因为SS1(P0_11)没有加上拉电阻
2、从机通信不正常，读数据波形不匹配；TXDAT在传输结束后需赋值0（或者~(0xAA|val)），下一次通信开始时，从机也会输出TXDAT的数据，导致和0xAA相与出错。


2018.5.19
1、LPC822的P0_10和P0_11管脚做SPI用时，需设置为GPIO模式，并外加上拉电阻；P0_10和P0_11本为I2C接口
2、SPI的LPCOpen例程中缺少SPI的ENABLE步骤
3、SPI做主机通信时，要设置多个SSEL的使能，否则会同时选通多个SSEL